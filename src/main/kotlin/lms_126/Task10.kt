package lms_126

fun task10() {
    /*
    Log in to ( https://outlook.live.com)
    IF A- Sign in
    IF B- create account
    Select "B" create  account

    Select Username:
    IF A = Outlook.com
    IF B = Hotmail.com
    Select B: ********@hotmail.com

    INPUT Password
    IF the password is length < than 6 characters long set error
    IF the password is > than 6 characters long continue
    Input  Name
    Input last Name
    Input Country/ Region
    Input Birth Day
    Input Birth Month
    Input Birth Year

    Input - Pick one square that shows two identical objects.

    Input A Exemple Pic
    Input B Exemple Pic
    Input C Exemple Pic
    Input D Exemple Pic

    IF Select A set error
    IF Select B set error
    IF Select C set succeed
    IF Select D set error

    Select D

    Output   Completed







     */
}
